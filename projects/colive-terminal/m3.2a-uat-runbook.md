# M3.2A "Composer" — Hardware UAT Run-Book

> **FINAL** — matches the accepted M3.2A spec `docs/superpowers/specs/2026-06-02-colive-terminal-m3.2a-composer-design.md`.
> Per the M3.0 spec **§0 (definition of done)**: M3.2A is DONE only when the **user** completes this on the real
> **G2 + R1** and signs off. Green tests + clean typecheck are the *precondition*, not done. Build agents produced
> the candidate; the controller re-verified from a clean tree (`npm ci && npm run typecheck && npm test` → typecheck
> exit 0, **391 tests passed / 36 files**, re-verified 2026-06-03 after the A2/A4/A6 fix pass); the user closes the rung on
> hardware. **Do not merge before sign-off.**
>
> **Pre-UAT self-test (2026-06-03):** the composer was rendered headlessly via the preview rig
> (`test/preview/m32a.preview.test.tsx` → `scripts/screenshots.sh`) and the screenshots reviewed — multiline authoring,
> the mid-line inverse-video cursor, multi-line paste, the slash menu + highlight, history recall, and a multi-line
> composer below a live transcript all render correctly; **no rendering bugs found.** What remains is what only hardware
> can prove: real keystroke delivery (Ctrl-J / ⌥-word / Fn-keys), whether your terminal forwards **SGR mouse** for the
> wheel, history persistence across a real desk restart, and the co-live regression.

## ⟳ Re-UAT after the fix pass (2026-06-03) — read this
First hardware UAT (2026-06-03) **passed A3, B1, B2** and A1 via Ctrl-J; it flagged **A2, A4, A6**. Fixes landed on
this branch (each TDD'd + independently reviewed, zero Core change):
- **A2 — Option+word-nav:** your VS Code terminal sends the *readline* form of Option+←/→ (`ESC-b`/`ESC-f`), which was
  unhandled. Added that form (kept the CSI form) **+ Option+Backspace = delete-word**. → re-test A2.
- **A4 — paste→arrow "jump":** this was a **real bug, not your terminal** — ↑/↓ used a stale React closure, so batched
  arrow events (auto-repeat / coalesced input) dropped all but the last and could fire history recall. Fixed (functional
  state updaters). An **opt-in logger** is available if anything still looks off: launch the desk with
  `COLIVE_A4_LOG=/tmp/colive-a4.log npm run dev -- desk`, reproduce, then send me `/tmp/colive-a4.log`. → re-test A4.
- **A6 — copy:** added a runtime **`/select` ⇄ `/scroll`** toggle (see A6 below). Also, in **VS Code specifically**, enable
  **`terminal.integrated.macOptionClickForcesSelection`** (Settings → search it) — that alone may restore Option-drag copy.
  The durable, terminal-agnostic **`/copy` (OSC 52)** is deferred to **M3.2B**.
- **A5 — more slash commands:** the full skill/CLI command set needs the Hub to *report* available commands — **deferred to
  M3.3** (the streaming-input refactor). The 5 local commands stay; this pass adds `/select` + `/scroll` (local).

Re-run focus: **A2, A4, A6** (A1/A3/B1/B2 already passed; no need to redo unless you want to).

### ✅ Round-2 results (2026-06-03) — A2 PASS; A4-step & A6-copy DEFERRED (user decision)
- **A2 — Option+word-nav: FULL PASS.** The readline `ESC-b`/`ESC-f` fix resolved it on the VS Code terminal.
- **A4 — one-line ↑/↓ stepping: still not working on hardware → DEFERRED, not mission-critical.** The functional-updater
  fix corrected a real (rig-verified) stale-closure bug, but per-line stepping still fails in the VS Code terminal, so an
  **additional cause remains unpinned**. Paste itself works (multi-line, no auto-submit). The `COLIVE_A4_LOG` logger stays
  wired to capture raw bytes + cursor-row when this is picked back up.
- **A6 — copy: still cannot copy → DEFERRED to a dedicated copy/paste phase** (to be scoped in the planning chat). The
  `/select`·`/scroll` toggle shipped but did not yield working copy in the user's VS Code setup. The whole copy/paste
  surface (mouse-reporting bypass, OSC 52 `/copy`, paste ergonomics) moves to its own phase.
- **A5 — full slash set:** unchanged — deferred to **M3.3** (needs Hub-reported commands).
- A1 / A3 / B1 / B2: PASS (round 1).

## What changed since M3.1 (read this first)
M3.1's input was a single-line buffer and the **arrow keys scrolled the transcript** (alt-screen mapped the wheel to ↑/↓).
M3.2A makes the input a real **composer** and **reassigns the arrows**:
- **↑ / ↓ now drive the input** (command history at the edges; cursor between draft lines) — they no longer scroll.
- **Transcript scrolling moves to the mouse wheel** (real SGR mouse reporting is now enabled) **and PageUp/PageDown**.
- **Cost (expected):** with mouse reporting on, selecting/copying transcript text now needs **Option-drag (macOS)** /
  Shift-drag — the vim/less/tmux convention. (If a terminal won't forward SGR mouse, the wheel won't scroll; PageUp/
  PageDown still do — see A6.)

## Keybinding map (as built) — with macOS-laptop equivalents
> On a Mac laptop there are no dedicated PageUp/PageDown/Home/End keys — they are `Fn`+arrow combos.

| Key | macOS-laptop | Action |
|---|---|---|
| **Enter** | Return | Submit the prompt |
| **Ctrl-J** / **`\`+Enter** | same | Insert a newline (keep composing). (Shift+Enter too **if** your terminal sends it) |
| **↑ / ↓** | ↑ / ↓ | History prev/next **when the cursor is on the top/bottom line**; otherwise **move the cursor** between draft lines |
| **← / →** | ← / → | Move cursor by character |
| **Option+← / Option+→** | ⌥+← / ⌥+→ | Move cursor by word |
| **Ctrl-A / Ctrl-E** | ⌃A / ⌃E | Cursor to line start / line end (the reliable line-nav keys) |
| **End** | **Fn+→** | Line end **while editing**; **re-pin the transcript to live when the input is empty** |
| **Backspace** | Delete | Delete the char before the cursor |
| **Ctrl-W** | ⌃W (or ⌥+Delete) | Delete the word before the cursor |
| **Mouse wheel** | two-finger trackpad scroll | Scroll the transcript (real mouse reporting) |
| **PageUp / PageDown** | **Fn+↑ / Fn+↓** | Page the transcript up / down |
| **`/` …** | same | Open the slash menu; ↑/↓ navigate, **Tab** completes, **Enter** runs, **Esc** closes |
| **`/select` · `/scroll`** | same | Mouse-mode toggle: `/select` turns mouse reporting **off** (native drag-select + Cmd-C; wheel off) · `/scroll` turns it back **on** (wheel scroll; default). Status line shows `select-mode` while off. |
| **Ctrl-O** | ⌃O | Toggle global verbose (unchanged from M3.1) |
| **Esc** | esc | Close the slash menu if open; else interrupt the running turn (M1) |
| **Ctrl-C** | ⌃C | Quit |

## Preconditions
- M3.2A branch (`colive-terminal-m3.2a`) checked out; `npm run typecheck` + `npm test` green (controller-verified from a
  clean tree — precondition only).
- **Launch (bin not installed yet):** from `colive-terminal/` run `npm run dev -- serve` in one terminal and
  `npm run dev -- desk` in a **tall** terminal. (A taller terminal = more transcript viewport, and the composer grows
  upward as you add lines, so give it room.)
- G2 glasses on + connected via the Even app (Terminal Mode); R1 ring paired.
- Model `claude-opus-4-8`, `permissionMode: default` (every tool prompts — answer with number keys at the desk or tap
  the ring). The composer items below are mostly **typing/keystroke** tests, so most don't need a tool prompt.
- Per-project history persists to `~/.colive/history/<project>.jsonl` (keyed by the Hub base URL). A3 deletes/uses it.

## Part A — Composer (at the desk terminal)

**A1. Multi-line authoring** — *Goal:* compose a prompt spanning several lines and submit it as one.
*Steps:* type a first line, press **Ctrl-J** to drop to a new line, type more; also try ending a line with **`\`** then
**Enter** (backslash-continuation) to add a line. Then press **Enter** to submit.
*Expected:* Ctrl-J / `\`+Enter add lines **without submitting**; the composer shows multiple rows (continuation rows are
indented under the `>` prompt) with a visible cursor; **Enter** submits the **joined** multi-line text as a single prompt
that reaches the agent intact.
[x] PASS — notes: via **Ctrl-J** (user won't use `\`+Enter; deprioritized). Round 1.

**A2. Cursor + word + line navigation and editing** — *Goal:* edit anywhere in the draft, not just at the end.
*Steps:* type a sentence; use **←/→** to move by char and **⌥+←/→** to move by word; jump to line start/end with
**Ctrl-A / Ctrl-E** (and try **Fn+→ / End**); insert text mid-line; delete a word with **Ctrl-W**; backspace across a
line boundary in a multi-line draft.
*Expected:* the cursor moves by char/word/line as expected; typed characters and deletes land **at the cursor**, not only
at the end; **Ctrl-W** removes the preceding word; backspace at column 0 merges with the previous line.
[x] PASS — notes: **FULL PASS** in round 2 after the readline `ESC-b`/`ESC-f` Option+word-nav fix (`29c93c8`).

**A3. Command history across a restart (per-project)** — *Goal:* recall previous prompts.
*Steps:* submit 2–3 distinct prompts (e.g. "say one", "say two", "say three"). **Quit the desk (Ctrl-C) and relaunch it**
(`npm run dev -- desk`). Press **↑** repeatedly, then **↓**.
*Expected:* **↑** recalls submitted prompts **newest-first**, then older; **↓** walks back toward the newest and, past it,
restores your in-progress draft. History **survives the restart** (it's persisted per project). Slash commands (e.g.
`/help`) are **not** recalled (prompts only). *(Optional: point the desk at a different Hub/project → it has its own
separate history.)*
[x] PASS — notes: full pass. Round 1.

**A4. Paste a multi-line block** — *Goal:* paste without premature submit.
*Steps:* copy a multi-line block from anywhere (e.g. a 3–4 line snippet) and paste it into the composer.
*Expected:* the paste fills the composer as **multiple lines** and does **NOT** auto-submit — even if the pasted text
contains carriage returns. You can then edit and submit with **Enter**.
[~] PARTIAL — notes: **paste works** (multi-line, no auto-submit) ✅. The post-paste **one-line ↑/↓ stepping still fails on
hardware → DEFERRED** (not mission-critical; user 2026-06-03). Fix `c537ac2` corrected a real stale-closure bug but an
additional cause remains; `COLIVE_A4_LOG` logger is wired for next time. See Round-2 results.

**A5. Slash-command menu** — *Goal:* discover/complete commands.
*Steps:* type **/** — a menu of commands appears (clear, compact, context, usage, help). Keep typing (e.g. **h**) to
filter. Use **↑/↓** to move the highlight; **Tab** to complete the highlighted command into the input; **Enter** to run it
(try **/help** and **/clear**). Open the menu again and press **Esc**.
*Expected:* the menu opens on `/` and filters as you type; ↑/↓ move the highlight (and the highlight resets to the top
when you re-filter); **Tab** completes; **Enter** runs the command **locally** (never sent to the model); **Esc** closes
the menu **without** interrupting the session.
[x] PASS — notes: the 5 local commands work (round 1). Pulling in the **full skill/CLI slash set is DEFERRED to M3.3**
(needs the Hub to report available commands). This pass added local `/select` + `/scroll`.

**A6. Transcript scroll (mouse wheel) + text selection** — *Goal:* scroll the transcript and select text now that arrows
drive the input. *Steps:* generate long output (paste below), then **scroll the wheel / two-finger trackpad**; also try
**PageUp/PageDown** (**Fn+↑/↓**); with the **input empty**, press **End** (**Fn+→**) to re-pin to live. Then try selecting
transcript text with **Option-drag**.
*Expected:* the **wheel/trackpad scrolls the transcript**; **PageUp/PageDown** page it; **End** (empty input) re-pins to the
bottom. **If your terminal does not forward SGR mouse** (some do not), the wheel won't scroll — **PageUp/PageDown still do**
(note which terminal behaves how).
*Copy (the fix-pass change):* with the wheel on, copying needs the terminal's mouse-reporting bypass (VS Code = **Option-drag
after enabling `terminal.integrated.macOptionClickForcesSelection`**; iTerm2 = Option; kitty/Ghostty/WezTerm/Alacritty =
**Shift**; Apple Terminal = none). The reliable, terminal-independent path: type **`/select`** → mouse reporting goes off and
the status line shows `select-mode` → drag-select normally and **Cmd-C** → type **`/scroll`** to restore wheel scrolling.
Verify both: (1) `/select` then drag-select + copy works and the wheel stops; (2) `/scroll` restores the wheel.
*(A durable `/copy` via OSC 52 — copy without selecting, works over Tailscale — will be designed in the dedicated phase.)*
▶ **Paste** (tool-free, deterministic long output):
```
Without using any tools, output a numbered list from 1 to 100. Each line is just the number, an en dash, and that number spelled out in English — e.g. "1 — one", "2 — two". Produce all 100 lines, nothing else.
```
[~] DEFERRED — notes (2026-06-03): **copy does not work — user could not copy anything** in the VS Code terminal, even with
the `/select` toggle. → **Whole copy/paste surface deferred to a dedicated phase** (scope in the planning chat). Not blocking
the rest of the composer.

## Part B — Co-live regression (light; input is desk-only) — real G2 + R1

> M3.2A changed only the **desk** input layer (zero Core/Hub change), so co-live should be untouched. These two checks
> confirm the M1/M3.1 co-live paths still hold.

**B1. Desk-typed prompt reaches the glasses and renders once** — *Trigger:* type a prompt at the desk with the session
open on the glasses. *Expected:* it streams to the glasses HUD as before and renders **exactly once** at the desk (no
double-render regression — M3.1 `8cd4535`).
▶ **Paste at the desk, session open on the glasses:**
```
Count slowly from 1 to 10, one number per line.
```
[x] PASS — notes: round 1.

**B2. Permission ring round-trip still dismisses on both surfaces** — *Trigger:* run a tool needing permission; tap the
ring to allow. *Expected:* tap = allow → tool runs → the permission prompt dismisses on **both** the desk and the glasses.
▶ **Paste at the desk, then tap the ring to allow when the prompt appears:**
```
Run exactly this shell command and report the result: touch /tmp/m32a_ring_test.txt && echo created
```
(Then `rm /tmp/m32a_ring_test.txt` to clean up.)
[x] PASS — notes: round 1.

## Sign-off
- **Round 1 (2026-06-03):** A1 (Ctrl-J), A3, B1, B2 PASS; A2/A4/A6 flagged; A5 = scope ask.
- **Round 2 (2026-06-03):** **A2 FULL PASS** after the fix pass. **A4 one-line ↑/↓ stepping** and **A6 copy** DEFERRED by
  user decision (A4 not mission-critical; A6 → a dedicated copy/paste phase). **A5** → M3.3 (Hub-reported commands).
- **Status for planning review:** the composer core — multi-line authoring, char/word/line cursor nav, per-project history,
  multi-line paste, slash menu, `/select`·`/scroll` toggle — is hardware-validated. Two items carried to planning:
  (1) **A4** post-paste per-line arrow stepping (deeper diagnosis; logger wired), (2) **A6** copy/paste (own phase). The
  planning chat decides whether M3.2A merges as-is with these deferrals, or whether A4/A6 split out.
- **Planning decision (2026-06-03, planner validation pass):** **MERGE AS-IS.** Validation audit confirmed: zero
  Core/Hub change, no test gaming, all three fix claims (A2/A4/A6) real in code, 391 tests / typecheck clean
  (clean-tree re-verified). Composer core is **hardware-signed-off**. The two deferrals are carried as their own
  follow-on work, NOT blockers:
  - **A4** (post-paste per-line ↑/↓ stepping on the VS Code terminal): net-new partial functionality, not a regression
    vs M3.1; `COLIVE_A4_LOG` diagnostic wired for the next diagnosis pass.
  - **A6** (copy/selection): M3.2A defaults mouse-reporting ON, which captures click-drag; the proper fix is the
    **terminal-agnostic OSC 52 `/copy`** in the **next dedicated copy/paste phase** (works with mouse-on + over Tailscale).
- **✅ Approved to merge to `main` — 2026-06-03.**
