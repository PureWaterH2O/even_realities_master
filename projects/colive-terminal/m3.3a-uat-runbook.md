# M3.3a UAT — Streaming-input Core (hardware G2 + R1)

Pure refactor: behaviour must be **identical to today**, plus a clean interrupt and self-heal.
M3.3a drives the SDK via **one persistent streaming `Query` per session** (was: one `query({prompt:string})`
per turn), with `Query.interrupt()` replacing the AbortController and lazy reopen-with-`resume` self-heal.
Emitted `CoLiveEvent`s are byte-identical; `SessionManager`/Hub/desk/glasses are unchanged.

> **DoD:** green tests + clean typecheck are the *precondition only*. M3.3a is DONE only when the user
> exercises it on the real G2 + R1 and signs off below.

## Setup (copy-paste)

Two terminal panes on the Mac, from the project dir. The Hub **prints a token + port in its banner** —
copy them into Pane B (the `serve` command auto-generates the token; do **not** pass `--token` to `serve`).

```bash
# Pane A — Hub + Core. Note the TOKEN + PORT printed in the banner (default port 3456).
cd ~/Documents/random_claude_stuff/even_realities/colive-terminal
npx tsx src/index.ts serve --host 127.0.0.1 --project-dir "$(pwd)"
```

```bash
# Pane B — desk client (fill in TOKEN + PORT from Pane A's banner)
cd ~/Documents/random_claude_stuff/even_realities/colive-terminal
npx tsx src/index.ts desk --host 127.0.0.1 --port <PORT> --token <TOKEN>
```

For the glasses walk (D4): start the Hub with Tailscale instead — `npx tsx src/index.ts serve` (no `--host`)
and connect the Even app as in the M2 runbook.

> Tip: to pin the token across restarts, prefix Pane A with `BRIDGE_TOKEN=<your-token>` (env var, not a flag).

## Walk

| # | Do this (copy-paste / keystroke) | Pass = |
|---|---|---|
| D1 | Send: `read package.json, then list src/core, then summarize the test setup` | Streaming text + tool round-trips + thinking + final idle render exactly as today |
| D2 | Send `say one`, then immediately send `say two` (queue mid-turn) | `two` runs only after `one` finishes (FIFO); both answered in order |
| D3 | Send `summarize every file in src/core in detail`, then press **Esc** mid-turn | Turn stops promptly; **no error banner** appears (clean interrupt); then send `now just say hi` → it runs on the same session immediately |
| D4 | From the **glasses**, send any prompt | Glasses send + receive events unchanged (co-live byte-compat) |
| D5 | Idle, toggle Wi-Fi off ~5s then on, then send `still there?` | Session self-heals — the prompt continues the same transcript (reopens with `resume`); a transient `error` may flash, then the turn proceeds |

> **D3 note:** the internal self-test found that the real `Query.interrupt()` ends a turn by flushing a
> *non-success* `result` (it does NOT throw). Without handling, that surfaced a spurious
> `[ede_diagnostic] …` **error banner** on every Esc. M3.3a suppresses it so a deliberate interrupt frames a
> **clean idle** (matching the old abort path) — D3 should show NO error banner. A genuine stream error
> (not an interrupt) still surfaces an error.

## Internal self-test (filled by the builder, Task 8 — LIVE local serve+desk, real SDK auth)

Driven through the real refactored Core via the Hub HTTP/SSE API (default permission mode):

- **Live local multi-tool turn:** ✅ PASS. Full event arc emitted live and identical to today:
  `user_prompt → status(busy/think_start/think_end/text_start/text_end) → thinking_delta → tool_start/tool_end
  (Read, Glob, Grep) → permission_request/permission_result → running_stats(10s heartbeat) → result(success) → status(idle)`.
  Zero spurious `error` events; `session_id` stable across turns. Desk render faithful — see screenshot.
- **Sequential turns (FIFO):** ✅ PASS. Multiple prompts on one session run in order on the **same** persistent
  query (no per-turn reopen); each `run()` resolves on its own `result`.
- **Clean interrupt + reuse:** ✅ PASS (after fix). Found + fixed the spurious-error regression above; a live Esc
  now yields **error count = 0** and a clean `status:idle`, and the **next prompt drives a fresh turn on the same
  session** (the M3.3a win) — verified live (`say hi → "Hi"` immediately after an interrupted essay).
- **Self-heal:** ✅ unit-verified (deterministic test: a dead query reopens with `resume:<sessionId>` on the next
  prompt and emits one `error`). The **live** trigger is a real query death / connectivity drop → exercised on
  hardware at **D5** (a best-effort local process-kill did not cleanly reproduce a query death, so D5 is the bar).
- **Screenshots:** `preview-out/shot-m33a-live.png` (live multi-tool turn, rendered by the real desk code) and
  `preview-out/shot-m33a-session.png` (deterministic desk-render regression, Task 7). The post-interrupt render
  (interrupted essay → `say hi → "Hi"` → idle) was captured as a desk frame and confirmed clean.

**Test/typecheck gates:** full suite **455 passed** (442 → 455, +13); `tsc --noEmit` exit 0; Core change
**CONFINED** to `src/core/session.ts` + `src/core/promptInbox.ts` (no `events.ts`/`sessionManager.ts`/`hub/`/`desk/`
change); no test deleted/weakened (5 renamed in the abort→`Query.interrupt()` / per-turn→persistent adaptation).

Sign-off: ___  Date: ___
